package main

import (
	"log"
	/*"net/http"

	"github.com/gorilla/mux"
	"dot-portal-go/db"
	"dot-portal-go/routes"
	"dot-portal-go/users"*/
)

func main() {

	log.Println("Starting Dot Portal...")

	/*
	db.Connect()
	users.Migrate(db.DB) // Create users table

	router := mux.NewRouter()
	routes.RegisterRoutes(router)

	log.Println("Server running on http://localhost:8080")
	log.Fatal(http.ListenAndServe(":8080", router))
	*/
}